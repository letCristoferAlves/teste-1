import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button:{
        paddingVertical: 8,
        backgroundColor: "#f00",
        width: '100%'
    },
    text:{
        fontSize: 12,
        color: "#fff",
        textAlign: 'center'
    }
});

export default styles;